runtime:
  backend: process
  workers: 4
  chunk_size: 1
  max_memory_ratio: 0.5

cache:
  data_enabled: true

output:
  directory: outputs/mining

logging:
  level: INFO
  task_logs: true
  heartbeat_seconds: 30
